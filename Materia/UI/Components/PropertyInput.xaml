<UserControl x:Class="Materia.UI.Components.PropertyInput"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Materia.UI.Components"
             mc:Ignorable="d" 
             d:DesignHeight="32" d:DesignWidth="256" Height="32" HorizontalAlignment="Stretch">
    <Grid>
        <TextBox Name="IField" Text="" Margin="2" Padding="2,4,2,0" CaretBrush="{StaticResource Primary}" Background="{StaticResource Overlay5}" BorderThickness="0" Foreground="{StaticResource TextLight}" GotFocus="IField_GotFocus" LostFocus="IField_LostFocus" TextChanged="TextInput_TextChanged" KeyDown="IField_KeyDown" />
    </Grid>
</UserControl>
